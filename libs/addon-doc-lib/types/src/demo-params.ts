/**
 * Query-param-backed demo settings for `<nxp-doc-demo>`.
 */
export interface NxpDocDemoParams {
  sandboxExpanded?: boolean;
  sandboxOpaque?: boolean;
  sandboxWidth?: number;
  darkMode?: boolean;
  updateOn?: 'blur' | 'change' | 'submit';
}
